# Puzzle Generator API — base URL for the Go backend
# In development the Next.js rewrite proxies /api/* to this URL.
# In production set the full URL including /api/v1.
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
